# ポイ活・転売・回線管理アプリ デザインリニューアル＋継続実装指示書 v3

## 0. 本指示書の位置づけ

v2指示書で実装を進めていたが、デザインを全面刷新する。
本指示書はデザイン変更箇所と未実装タスクをまとめたもの。
v2指示書のうち、本指示書に記載のない仕様（データモデル、バリデーション、計算ロジック、保存方針、Supabase実装方針など）はすべてそのまま有効。

---

## 1. 実装済み（変更不要な部分）

以下はv2で実装完了しており、ロジック・データ構造はそのまま維持すること。

- Supabase schema SQL / seed SQL
- 型定義 (database.types.ts)
- ポイ活 CRUD（server actions + フォーム + バリデーション）
- 転売 CRUD（server actions + フォーム + バリデーション）
- 回線 CRUD（server actions + フォーム + 月額履歴サブフォーム + バリデーション）
- 設定 CRUD（4カテゴリ切替 + 有効/無効切替）
- ダッシュボード集計ロジック（今年/今月/月別/カテゴリ別）
- 計算 utility (calculations.ts)
- analytics.ts（フィルタ・集計関数）
- Zod スキーマ (validation/forms.ts)
- lib/utils.ts（formatCurrency, formatDate, maskPhoneNumber, parseNumberInput）
- lib/constants.ts

---

## 2. デザイン刷新：カラーテーマ

### 2-1. ダークモード（デフォルト）
Navy × Gold テーマを採用する。

```
bg:          #0c1222
surface:     #131b2e
surfaceAlt:  #1a2540
border:      rgba(255,255,255,0.06)
text:        #e0e4ef
textSub:     rgba(224,228,239,0.45)
accent:      #f5b942
accentBg:    rgba(245,185,66,0.12)
profit:      #4ade80
profitBg:    rgba(74,222,128,0.12)
prospect:    #f5b942
prospectBg:  rgba(245,185,66,0.12)
muted:       #64748b
mutedBg:     rgba(100,116,139,0.15)
nav:         #0a0f1c
navActive:   rgba(245,185,66,0.14)
```

### 2-2. ライトモード
Cool White × Indigo テーマを採用する。

```
bg:          #f5f7fa
surface:     #ffffff
surfaceHover:#eef1f6
border:      rgba(0,0,0,0.06)
text:        #0f172a
textSub:     rgba(15,23,42,0.5)
accent:      #3730a3
accentBg:    rgba(55,48,163,0.07)
profit:      #16a34a
prospect:    #c2820e
nav:         #eef0f5
navActive:   rgba(55,48,163,0.09)
navActiveText:#3730a3
pillActive:  #3730a3
pillActiveText:#ffffff
pillInactive:#e5e7ee
```

### 2-3. テーマ切替

- サイドバーまたはヘッダーにダーク/ライト切替トグルを設置する
- ユーザーの選択を localStorage に保存し、次回アクセス時に復元する
- OS のプリファレンスにも対応する（初回はOSに従う）
- Tailwind の `darkMode: 'class'` を使用し、`<html>` タグに `dark` クラスを付け外しする

### 2-4. Tailwind 設定の変更

tailwind.config.ts の colors を以下に差し替える。

```typescript
darkMode: 'class',
theme: {
  extend: {
    colors: {
      // ダーク用
      dark: {
        bg: '#0c1222',
        surface: '#131b2e',
        surfaceAlt: '#1a2540',
        nav: '#0a0f1c',
      },
      // ライト用
      light: {
        bg: '#f5f7fa',
        surface: '#ffffff',
        nav: '#eef0f5',
      },
      // 共通セマンティック（CSS変数で切替推奨）
      accent: 'var(--color-accent)',
      accentBg: 'var(--color-accent-bg)',
      profit: 'var(--color-profit)',
      prospect: 'var(--color-prospect)',
      muted: 'var(--color-muted)',
      ink: 'var(--color-text)',
      'ink-sub': 'var(--color-text-sub)',
      canvas: 'var(--color-bg)',
      surface: 'var(--color-surface)',
    },
    boxShadow: {
      panel: '0 12px 32px rgba(0,0,0,0.15)',
    },
  },
},
```

### 2-5. globals.css の変更

CSS変数でテーマを切り替える方式にする。

```css
:root {
  --color-bg: #f5f7fa;
  --color-surface: #ffffff;
  --color-nav: #eef0f5;
  --color-border: rgba(0,0,0,0.06);
  --color-text: #0f172a;
  --color-text-sub: rgba(15,23,42,0.5);
  --color-accent: #3730a3;
  --color-accent-bg: rgba(55,48,163,0.07);
  --color-profit: #16a34a;
  --color-prospect: #c2820e;
  --color-muted: #64748b;
  --color-muted-bg: rgba(100,116,139,0.15);
  --color-nav-active: rgba(55,48,163,0.09);
  --color-nav-active-text: #3730a3;
}

.dark {
  --color-bg: #0c1222;
  --color-surface: #131b2e;
  --color-nav: #0a0f1c;
  --color-border: rgba(255,255,255,0.06);
  --color-text: #e0e4ef;
  --color-text-sub: rgba(224,228,239,0.45);
  --color-accent: #f5b942;
  --color-accent-bg: rgba(245,185,66,0.12);
  --color-profit: #4ade80;
  --color-prospect: #f5b942;
  --color-muted: #64748b;
  --color-muted-bg: rgba(100,116,139,0.15);
  --color-nav-active: rgba(245,185,66,0.14);
  --color-nav-active-text: #f5b942;
}

body {
  background: var(--color-bg);
  color: var(--color-text);
}
```

---

## 3. UI変更：完了済タブの分離

### 3-1. 対象画面
ポイ活管理、転売管理、回線管理の3画面。

### 3-2. 仕様
各画面の一覧上部に「進行中」「完了済」のタブを設置する。

- 進行中タブ: is_completed = false のレコードのみ表示（デフォルト表示）
- 完了済タブ: is_completed = true のレコードのみ表示
- タブには件数バッジを表示する（例: 進行中 (3) / 完了済 (5)）
- フィルタ（年・月・区分等）はタブごとに独立して機能する

### 3-3. 既存実装の活用
v2の実装で以下の関数がすでに存在する場合はそのまま活用すること。
- filterPointActivitiesByTab / getPointActivityTabCounts
- filterResaleTransactionsByTab / getResaleTabCounts
- filterMobileLinesByTab / getMobileLineTabCounts

SegmentedTabs コンポーネントも既存のものを流用する。
デザインのみ新テーマに合わせて更新する。

---

## 4. UI変更：コンポーネントのテーマ対応

### 4-1. 変更が必要な共通コンポーネント

以下のコンポーネントをダーク/ライト両対応に更新する。
Tailwind のユーティリティクラスで `dark:` プレフィックスを使うか、CSS変数を参照する形にする。

- app-shell.tsx（サイドバー背景・ナビゲーションの色）
- section-card.tsx（カード背景・ボーダー）
- status-badge.tsx（バッジ色：完了=profit系、見込み=prospect系、無効=muted系、案件=accent系）
- summary-stat-card.tsx（KPIカードの配色）
- segmented-tabs.tsx（タブのアクティブ色）
- ui/button.tsx（primary=accent色、secondary=surface色）
- ui/field.tsx, ui/input.tsx, ui/select.tsx, ui/textarea.tsx（フォーム要素の背景・ボーダー）
- configuration-notice.tsx（警告バナーの配色）

### 4-2. AppShell の変更

- サイドバーにダーク/ライトモード切替トグルを追加
- ロゴ部分「POINTTENBAI」のカラーをaccent色にする
- アクティブなナビリンクの背景をnavActive、テキスト色をaccent色にする
- 非アクティブのナビリンクはtextSub色

### 4-3. バッジ色の統一

| 状態 | ダークモード | ライトモード |
|------|-------------|-------------|
| 完了 | profit (#4ade80) + profitBg | profit (#16a34a) + rgba(22,163,74,0.1) |
| 見込み | prospect (#f5b942) + prospectBg | prospect (#c2820e) + rgba(194,130,14,0.1) |
| 維持中 | muted (#64748b) + mutedBg | muted (#64748b) + rgba(100,116,139,0.1) |
| 解約済 | muted (#64748b) + mutedBg | muted (#64748b) + rgba(100,116,139,0.1) |
| 案件回線 | accent (#f5b942) + accentBg | accent (#3730a3) + accentBg |
| 通常回線 | muted (#64748b) + mutedBg | muted (#64748b) + mutedBg |
| 有効 | profit系 | profit系 |
| 無効 | muted系 | muted系 |

---

## 5. 残タスク一覧（未実装 or 要修正）

### 優先度A（デザインリニューアルに伴う修正）

- [ ] tailwind.config.ts カラー差し替え + darkMode: 'class' 追加
- [ ] globals.css CSS変数方式に書き換え
- [ ] ThemeProvider コンポーネント新規作成（localStorage + OS preference対応）
- [ ] app-shell.tsx テーマ対応 + トグル追加
- [ ] 全共通コンポーネントのテーマ対応（上記4-1参照）
- [ ] 各管理画面のスタイルをテーマ対応に更新
- [ ] ダッシュボード画面のKPIカード・カテゴリカード配色更新

### 優先度B（v2で未完了の機能）

- [ ] ダッシュボード期間フィルタの高度化（先月・直近3か月・任意期間指定）
- [ ] スマホ表示のカード形式調整（レスポンシブ最適化）
- [ ] フィルタUI強化（年・月プルダウン、カテゴリフィルタの実装確認）

### 優先度C（将来フェーズ）

- [ ] CSV出力
- [ ] 税務出力
- [ ] PWA化
- [ ] RLS導入

---

## 6. ファイル変更マップ

変更が必要なファイルの一覧。ロジック変更は不要で、スタイルのみ更新するものが大半。

| ファイル | 変更内容 |
|---------|---------|
| tailwind.config.ts | カラー定義差し替え、darkMode追加 |
| src/app/globals.css | CSS変数方式に全面書き換え |
| src/app/layout.tsx | ThemeProvider追加 |
| src/components/app-shell.tsx | テーマ対応 + トグルUI追加 |
| src/components/section-card.tsx | 背景・ボーダーをCSS変数参照に |
| src/components/status-badge.tsx | バッジ色をテーマ対応に |
| src/components/summary-stat-card.tsx | KPIカード配色更新 |
| src/components/segmented-tabs.tsx | タブ色をテーマ対応に |
| src/components/configuration-notice.tsx | 警告色をテーマ対応に |
| src/components/ui/button.tsx | ボタン色をテーマ対応に |
| src/components/ui/field.tsx | ラベル色をテーマ対応に |
| src/components/ui/input.tsx | 入力欄の背景・ボーダーをテーマ対応に |
| src/components/ui/select.tsx | 同上 |
| src/components/ui/textarea.tsx | 同上 |
| src/features/point-activities/point-activity-manager.tsx | スタイルのみ更新 |
| src/features/resale/resale-manager.tsx | スタイルのみ更新 |
| src/features/mobile-lines/mobile-line-manager.tsx | スタイルのみ更新 |
| src/features/settings/settings-manager.tsx | スタイルのみ更新 |
| src/app/page.tsx（ダッシュボード） | スタイルのみ更新 |
| 新規: src/components/theme-provider.tsx | ダーク/ライト切替ロジック |

---

## 7. 実装の進め方

1. まず tailwind.config.ts と globals.css を更新する
2. ThemeProvider を作成し layout.tsx に組み込む
3. app-shell.tsx をテーマ対応にする（トグル含む）
4. 共通UIコンポーネントを順にテーマ対応にする
5. 各features配下の画面コンポーネントのスタイルを更新する
6. ダッシュボードのスタイルを更新する
7. スマホ表示を確認・調整する

ロジックには手を入れず、スタイルだけを差し替えること。
既存のCRUD・バリデーション・計算ロジックが壊れていないか、各画面で動作確認を行うこと。

---

## 8. 補足：v2指示書からの変更サマリ

| 項目 | v2 | v3 |
|------|----|----|
| テーマ | ライトのみ（緑×ベージュ） | ダーク(Navy×Gold) + ライト(白×Indigo) 切替 |
| 一覧表示 | 全件一括表示 + フィルタで完了/未完了切替 | 「進行中」「完了済」タブで分離 |
| カラー基調 | accent=#1d6f5f, canvas=#f5f2e9 | ダーク: accent=#f5b942, bg=#0c1222 / ライト: accent=#3730a3, bg=#f5f7fa |
| バッジ | 緑・グレー・オレンジ系 | テーマに応じて自動切替 |
| サイドバー | 白半透明 | ダーク: #0a0f1c / ライト: #eef0f5 |
